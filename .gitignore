# ── COMPANY DATA — NEVER COMMIT ────────────────────────────────────
# Written before the folder has anything in it, on purpose.
data/company/
*.xlsx
*.xls

# Regenerated from code, so not worth committing
data/synthetic/
data/public/*.csv
data/public/*.zip

# Model artifacts
models/
reports/

# Node modules for the UI checks. Installed INTO tests/ui/ rather than
# globally, so `rm -rf` on this repo leaves nothing behind on the machine.
tests/ui/node_modules/
tests/ui/package-lock.json

.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
.coverage

.env
.DS_Store
